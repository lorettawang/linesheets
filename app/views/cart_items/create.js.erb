<% if @cart.errors.any? || @cart_item.errors.any? %>
alert("This product is currently unavailable. Sorry!")
<% else %>
  $(".cart-text").html("<%= escape_javascript(render 'layouts/cart_text') %>")
<% end %>